{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci",
  "routes": [
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
